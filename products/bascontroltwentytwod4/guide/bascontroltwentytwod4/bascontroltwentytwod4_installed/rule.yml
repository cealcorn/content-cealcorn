documentation_complete: true

title: Ensure Bascontroltwentytwod 4 placeholder package is installed
description: |
    This placeholder rule exists only to validate product build plumbing.
rationale: |
    A minimal rule is required so the product generates XCCDF and OVAL content.
severity: low

template:
    name: package_installed
    vars:
        pkgname: bash
